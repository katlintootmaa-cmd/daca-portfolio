name: Week 8 Pipeline

on:
  push:
    paths:
      - "week-08-API-Pipeline/team/**"
      - ".github/workflows/week8-pipeline.yml"
  pull_request:
    paths:
      - "week-08-API-Pipeline/team/**"
      - ".github/workflows/week8-pipeline.yml"
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:
    inputs:
      analysis_date:
        description: "Analysis end date in YYYY-MM-DD format"
        required: false
        default: "2025-02-28"

jobs:
  run-week8-pipeline:
    name: Run Week 8 API pipeline
    runs-on: ubuntu-latest

    env:
      ANALYSIS_DATE: ${{ github.event.inputs.analysis_date || '2025-02-28' }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install pipeline dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pandas plotly python-dotenv supabase PyYAML pytest

      - name: Show data source mode
        run: |
          if [ -n "$SUPABASE_URL" ] && [ -n "$SUPABASE_ANON_KEY" ]; then
            echo "Supabase secrets are configured. The pipeline will try the API first."
          else
            echo "Supabase secrets are missing. The pipeline will use its fallback data path."
          fi

      - name: Compile Week 8 Python files
        run: |
          python -m py_compile \
            week-08-API-Pipeline/team/data_fetcher.py \
            week-08-API-Pipeline/team/notifications.py \
            week-08-API-Pipeline/team/transform.py \
            week-08-API-Pipeline/team/visualize_export.py \
            week-08-API-Pipeline/team/pipeline.py

      - name: Run Week 8 tests
        run: python -m pytest week-08-API-Pipeline/team/tests

      - name: Run team pipeline
        working-directory: week-08-API-Pipeline/team
        run: |
          mkdir -p output logs
          python pipeline.py --date "$ANALYSIS_DATE"

      - name: Show generated files
        if: always()
        run: |
          echo "Generated Week 8 files:"
          find "${{ github.workspace }}/week-08-API-Pipeline/team" -maxdepth 2 -type f | sort

      - name: Upload pipeline outputs
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: week8-pipeline-output
          path: |
            ${{ github.workspace }}/week-08-API-Pipeline/team/output/
            ${{ github.workspace }}/week-08-API-Pipeline/team/logs/
          if-no-files-found: warn
