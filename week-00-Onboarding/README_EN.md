# DACA Week 0: Onboarding

Estonian version: [README.md](README.md)

**Task:** as an assignment, I built my first vertical slice solution (`Supabase + GitHub + VS Code`)

## 1) Setup
- Supabase project `urbanstyle-daca`
- `team_members` table + 4 rows (me + Toomas + Kristi + Anna)
- GitHub repository `daca-portfolio` (public)
- VS Code + Python + SQLTools

## 2) Main Lessons Learned
- A database replaces Excel: the right "single source of truth"
- Git = version control + commit = work checkpoint
- GitHub = portfolio visibility + collaboration
- "Vertical slice": code in version control + data in the cloud + running a query from the editor

## 3) Next
- week-1 SQL queries (JOIN, GROUP BY, aggregation)
- applying data in practice (dashboard or report from Supabase tables)
- preparation for the demo session

> Weekly summary: the first tools are running and the workflow is in place; the focus now is on refining data logic and deeper analysis.
